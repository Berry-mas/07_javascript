function filterRange(arr, a, b) {
  if (arr.indexOf(a) > arr.indexOf(b)) {
    const leftIndex = arr.indexOf(b)
    const rigthIndex = arr.indexOf(b)
  }
  for (let i = )
}


const arr = [5, 3, 8, 1, 10, 4];

filterRange(arr, 1, 5);

console.log(arr);